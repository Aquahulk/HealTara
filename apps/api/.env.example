PORT=3001

# PostgreSQL connection URL
# Neon example: postgresql://<user>:<password>@<host>/<db>?sslmode=require&connection_limit=5&pool_timeout=30
# Local example: postgresql://postgres:postgres@localhost:5432/docproc
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require&connection_limit=5&pool_timeout=30

# JWT secret used to sign/verify tokens (set to a strong random string)
JWT_SECRET=change-me-dev-secret

# Comma-separated origins allowed by CORS. Include your Next dev port.
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003

# Optionally set a primary domain to allow wildcard subdomains for local testing
# CORS_PRIMARY_DOMAIN=